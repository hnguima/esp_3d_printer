syntax = "proto2";

import "nanopb.proto";

message WifiConfig {
  required bool enabled = 1 [default = false];
  required string ssid = 2 [default = "CLARO_2GA8652A", (nanopb).max_size = 64];
  required string password = 3 [default = "38A8652A", (nanopb).max_size = 64];
}
